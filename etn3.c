#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main(){
    char cadena [10]
    printf("un int ocupa %d bytes", sizeo(int));
    printf (" un char ocuoa %d bytes",sizeo(char));
    printf("un float ocupa %d ", sizeo(ffloat));
    printf ("un double ocupa %d ", sizeof(double));
    printf ("una cadena ocupa %d" sizeof(cadena ));
}